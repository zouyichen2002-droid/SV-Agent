# SynthV 创作链项目 — 交接文件

> ⚠️ **本文件是 2026-08-20 上一个会话的交接记录，保留作历史。其中若干事实已被接手会话复核修正，以下面三份为准：**
>
> - [`specs/benchmark-facts-chaosheng.md`](specs/benchmark-facts-chaosheng.md) — 基准曲事实，逐条标注证据等级。
>   **§5.2「主唱/和声混在一条 stem 是总闸门」已被推翻**（和声区间内覆盖反而更高）；
>   §4.1 的「42 演唱行」实际是 52 行，差额 10 行经判定确为无人声。
> - [`specs/environment-notes.md`](specs/environment-notes.md) — 环境，**三条已过期**：
>   torchaudio 可与 torch 2.13 共存且 `forced_align` 可用；HuggingFace 现在 56.8MB/s；
>   新增一条 numpy LAPACK 与 torch OpenMP 冲突会直接 abort。
> - [`specs/adr/`](specs/adr/) — ADR-0001..0004。§5.5 的验收目标已细化为 `[gates.*]` 配置。
>
> §0（协作模式）与 §3（桥的能力边界）仍然有效。

> 交接给下一个 Claude Code 会话。**请先完整读完本文件再动手。**
> 生成时间：2026-08-20　　上游基础：`SynthVCopilot/synthv-agent-bridge` v0.3.1

---

## 0. 协作模式：他是架构师，你是实现者

**用户是 27 届 NEU ECE 硕士，音频信号处理出身，正准备回国找 agent/AI 应用开发的工作。这个项目要写进简历。**

**他已明确决定：整个项目他不亲自写代码。** 理由（他的原话要点）：agent 岗主要考架构和解决问题，局部最优交给 AI 即可；算法能力他通过刷题单独补。**这是他权衡后的决定，不要反复劝，按此执行。**

因此实际分工：

- **代码由你写。** 模块实现、脚手架、脚本、测试，都你来。
- **决策由他做。** 这是这个模式能不能立住的关键 —— 见下。

### 0.1 硬性要求：每个非平凡决策走"选项 + 取舍 → 他拍板 → 记 ADR"

面试深挖只问两类问题：**「为什么选 X 不选 Y」**和**「失败时你怎么发现的」**。所以：

- 遇到技术岔路（选哪个 f0 估计器、分离模型、要不要量化到网格、eval 门槛定多少），**不要自己默默定了就往下做**。列出选项、各自代价、你的推荐，让他拍板。
- 拍板后写进 `specs/adr/` 决策日志：**背景 → 选项 → 他选了什么 → 理由 → 后续如果被推翻是因为什么证据**。
- 这份 ADR 日志是这个项目最值钱的面试产出，比能跑的 demo 值钱。

**第一条 ADR 已经有现成素材**：上一个会话先做 DP 分段（对实测 f0 50–64%），被量化否掉后转向 CTC 强制对齐（曲线 A 有数据处偏差 +0.22）。含完整的失败证据链，直接可写。

### 0.2 他的两个目标（决定项目形状，见 §1.1）

1. **解决自身的创作问题** —— 翻唱和独立创作都要真能用，不是玩具
2. **作为 agent 架构思维的全栈演练场** —— 召回、记忆、MCP、tool 这些子系统要真实存在并被用到

配套：**他有一个 26 周自学计划（2026-08-19 起，材料在 `E:\自学开发\`）。这个项目应作为那条主线的实践，不要另起一摊。**

---

## 1. 项目目标

在 `synthv-agent-bridge`（第三方 MCP 服务）之上，补全整个创作链条：

| 线 | 内容 | 验收依据 |
|---|---|---|
| **线 1 · 翻唱** | 从参考音频 + 歌词，产出可唱的 SynthV 主唱轨 | **《潮声回响》为基准曲**，见 §5 的量化目标 |
| **线 2 · 独立创作** | 从动机/歌词出发的作曲、编曲、调声辅助 | **待定** —— 用户说后面再给 |

### 1.1 项目形状：五个子系统的全栈 agent 系统

按 §0.2 的第二个目标，这个项目**不是**一条转写管线，而是一个音乐创作 agent 系统，桥只是其中一个工具。五个子系统都要有真实载体（**不要为了凑架构而硬造用例**）：

| 子系统 | 真实载体 |
|---|---|
| **MCP** | 消费 `synthv-agent-bridge`（已有）**+ 自己实现一个 MCP server**（乐理/语料侧）。自己写过 server 的面试信号远强于只会调用 |
| **Tool 设计** | 工具面 schema、守卫、撤销边界、单写者冲突处理。桥是现成范本（见 §3），他的层设计自己的工具面 |
| **记忆** | 每首歌的工程状态、他的调声惯例与风格偏好、历史决策。创作项目的记忆需求是天然的 |
| **召回** | 已装的四个技能是真语料（`synthv-tuning` / `composition-arrangement` / `lyric-writing` / `synthv-agent`），加上他的历史工程与 ADR 日志 |
| **编排 + eval** | §6 的六阶段带门槛管线 + **跨估计器交叉验证**（用 A 建、用 B 验，不是循环验证）+ **无证据不猜、显式上报不确定区间** |

**这个形状同时覆盖两条线** —— 记忆、召回、编排三层共享，翻唱和独立创作只是不同入口。

### 1.2 范围风险

用户原话是"完善所有的功能"，这是**无界的**，也是本项目最大的风险：面试官探深度，一个铺得很开没做完的项目不如一个小而完整的。建议早期就和他约定"第一个可交付里程碑"的边界，并把它写进 ADR。

---

## 2. 环境（已验证）

| 项 | 值 |
|---|---|
| OS | Windows 11 Home China 10.0.26200 |
| Synthesizer V Studio 2 Pro | **2.2.1**，装在 `G:\Synthesizer V Studio 2 Pro\` |
| 声库 | `MEDIUM5·Stardust`（星尘），mandarin / xsampa / SVR3 v110 |
| Node.js | 24.19.0，`C:\Program Files\nodejs\`（已在系统 PATH，但某些 shell 的 PATH 是旧的，必要时用绝对路径） |
| Python | 3.13（miniconda，`G:\miniconda`） |
| 已装 py 包 | librosa 1.0.0 / numpy 2.3.4 / scipy 1.16.3 / soundfile 0.14.0 / torch 2.13.0+cpu / transformers 5.15.1 |
| **未装且不要装** | **torchaudio** —— pypi 最高 2.11.0，会把 torch 2.13.0 降级弄坏。CTC Viterbi 自己实现即可（已有实现，见 §6） |
| 桥仓库 | `E:\SV_MCP`（v0.3.1，commit `5c51bd9`，Apache-2.0） |
| 技能 | `C:\Users\admin\.claude\skills\`：`synthv-agent` / `synthv-tuning` / `lyric-writing` / `composition-arrangement` |
| MCP 注册 | 项目级 `E:\潮声回响\.mcp.json` → `node E:/SV_MCP/dist/src/cli.js` |
| 网络 | pypi 通；HuggingFace 通但**慢**（440–560 KB/s 最好情况，会衰减到 23 KB/s） |

### 2.1 许可边界（面试里会被问，别含糊）

- **桥**（`synthv-agent-bridge`）：Apache-2.0，可自由使用修改分发。
- **技能**（`SynthVCopilot/SKILLS`）：Apache-2.0 + Commons Clause + 附加条款，**source-available 不是开源**。个人使用、公司内部、付费工作都允许；**不可转卖或打包进付费产品/插件市场**。产出物（歌词/编曲/调校工程）不受限，作者不主张权利。
- **用户的贡献是在别人的 MCP Runtime 之上的一层。** 简历和面试里必须说清这个边界 —— 说清反而显专业，含糊很难看。

---

## 3. 桥的能力边界（实测，不要重新踩）

### 3.1 六个工具

`sv_status` / `sv_describe` / `sv_query` / `sv_command` / `sv_ui` / `sv_review`

动作面：**17 read + 29 edit + 7 delete + 9 ui + 2 transaction**。用 `sv_describe` 带 `action` 参数取单个动作的 JIT schema。

### 3.2 官方 API 缺口 —— 做不到的事

- **保存工程**（桥写入只在内存，必须人工 Ctrl+S）
- **渲染 / 导出 WAV**
- **读当前声库显示名**（工程文件里有 `mainRef.database.name`，但 API 读不到；每次开工需人工告知）
- 枚举未修改过的默认 Vocal Mode 名
- active Retake 内容、Track effect-chain、伴奏音频路径
- **桥不解析 `.svp`**（ADR 0001：SynthV 是唯一权威）。离线分析 `.svp` 是允许的，但不能让桥去补这个洞。

### 3.3 已禁用的动作（SynthV 2.2.1 上可复现原生崩溃）

```
clone_track_shell / clone_track / create_harmony_track  → availability: experimentalDisabled
reason: reproducible SynthV 2.2.1 native crashes during
        isolated Group Undo and Track shell creation
apply_transaction → 有 0xc0000005 崩溃史，绕开
```

**建新轨只能用普通 `add_track`，新轨没有声库，必须人工指派。** 和声轨不能依赖整轨复制，只能用 linked group reference。

### 3.4 读取格式坑

`get_track_notes` 音符多时自动切 dense 列式投影：`notes` 变成 `{columns:[...], rows:[[...]]}`（`noteFormat: "rows"`），**不是对象数组**。解码：`dict(zip(columns, row))`。

### 3.5 写入协议

- 单写者，冲突返回 `BRIDGE_BUSY`
- 一个逻辑命令 = 一条 SynthV 撤销记录；**撤销记录只是恢复边界，不承诺自动回滚**
- 写前先 `sv_query ... contextMode=writeIntent` 取 contextId，再传给 `sv_command`
- 共享 Group（referenceCount > 1）内容写入默认拒绝
- `edit_notes` 协议上限 512，但 **describe 明确警告每批 ≤60**，宿主对大批量脆弱
- 曲谱导入 `import_monophonic_score` ≤512 音符/次，只接受本地绝对路径的 `.xml/.musicxml/.mxl/.mid/.midi`，拒绝 URL 和 `.svp`，需 `rightsConfirmed: true`

**实测可靠性：本次会话所有写入零失败**（174/174、300/300 逐字段一致，每次 `verified: true`，隔离性每次成立）。**烂掉的从来不是桥，是转写。**

---

## 4. 基准曲《潮声回响》的素材与事实

洛天依原曲翻唱重制，用星尘。工程 `E:\潮声回响\翻唱重制\潮声回响-86BPM.svp`。

### 4.1 已测定的事实

| 项 | 值 |
|---|---|
| 真实 tempo | **86.00 BPM**（实测 85.995），原工程标的 112.32 是错的 |
| 拍号 / 首拍 | 4/4；第一拍 0.240s |
| 音频总长 | 229.459592s（与工程 `durationSeconds` 精确吻合） |
| 干声参考 | `E:\潮声回响\人声分离\htdemucs\...\vocals.wav`，44.1kHz 立体声 PCM16 |
| 伴奏 | 同目录 `no_vocals.wav` |
| 歌词 | 完整 LRC 存于 `E:\潮声回响\翻唱重制\lyrics.lrc`（用户提供）。42 演唱行 = **33 主唱行（315 字）+ 9 和声行（95 字，带括号）**，23.54–198.66s。**属版权内容，已被 `.gitignore` 排除，不要提交** |
| LRC 对齐 | **全局 offset ≈ 0**（前四行残差 +0.34/+0.08/−0.28/+0.41s，offset 0 的平均残差 239ms 是所有候选里最小的）。**注意：只做了全局估计，逐行独立偏移未做** |

### 4.2 时间修正因子 1.0028571 —— 经两条独立证据链证实

`CORR = 112.32 / 112.00 = 1.0028571`

组 A（轨1 的 174 音符）的时间基是 112.00，而工程 tempo 是 112.32，需乘 CORR 修正。证据：

1. **组间比值**：0.9971512 ± 2e-06
2. **对未修改 f0 的音高吻合度提升**：三个独立门限下分别 **+11.0 / +14.3 / +13.2 个百分点**

两条路径完全独立。**这是本项目唯一被双重证实的定量结论，可以放心当地基。**

### 4.3 原工程被破坏了（重要）

`C:\Users\admin\Desktop\临时文件5\潮声回响-星尘.svp` 在迁移**之后**被删掉了 group2 开头 **61 个音符**（覆盖 3.365–53.135s），签名符合用户自制脚本 `DeleteNotesBeforePlayhead.js`。

- 现状：174 + **298** = 472 音符
- 完好版：174 + **359** = 533 音符，在 `C:\Users\admin\AppData\Roaming\Dreamtonics\Synthesizer V Studio 2\recovery\2026-08-19\潮声回响-星尘_2026-08-19_04-32.svp`
- **`潮声回响-86BPM.svp` 是完好的那份**（533 音符，绝对时间逐个精确保留）。**绝对不要重跑迁移脚本** —— 现在的源只有 472 音符，重跑会永久丢掉那 61 个。
- 备份：`潮声回响-86BPM.backup-20260820.svp`（sha256 校验过）

### 4.4 工程现状（7 条轨道）

| # | 轨道 | 内容 | 状态 |
|---|---|---|---|
| 1 | 未命名音轨 | vocals 174 音符 + 1860 pitchControls | audio2score 原始转写，**时间基错误**，勿直接用 |
| 2 | 未命名音轨 1 | 音频 `no_vocals.wav` | 伴奏，pan −0.21 |
| 3 | 未命名音轨 2 | 音频 `vocals.wav` | 干声参考，gain −9.3dB |
| 4 | 未命名音轨 3 | vocals 359 音符 + 3383 pitchControls | audio2score 第二次转写，**质量差**：39% 音符落在无声区、31.5% 音高偏差 >3 半音 |
| 5 | 主唱-轨1时间修正-v1 | 174 音符 | **当前音高质量最好的基线**，见 §5 |
| 6 | 主唱-全曲骨架-v2-待校 | 286 音符 | DP 分段版，已被 v4 取代，**可删** |
| 7 | 主唱-CTC对齐-v4-半数待校 | 300 音符 | 上次主交付，用户听后评价"效果一般、很多歌词错位" |

**注意：轨 5/6/7 的写入只在内存 + 用户已存过盘一次（2026-08-20 01:00）。之后的写入是否存盘需确认。** 桥不能保存。

---

## 5. 线 1（翻唱）实验成果 —— 准确表述

### 5.1 成功的部分

**CTC 强制对齐这一步是成功的，可以复用：**

- 逐 LRC 行对齐（每行窗口 2–9s、目标 4–16 字），**33/33 行全部完成**
- **300/315 字直接对齐（95.2%）**，全曲总耗时 **11 秒**（CPU，16 线程）
- 边界质量有硬证据：**在曲线 A 有数据的音符上，音高偏差仅 +0.22 半音**（对照：DP 分段版是 +2.6）
- CTC 是尖峰式的：每字只在一帧爆发（跨度 20ms）。**尖峰给的是起音**，时长取"到下一字起音"

### 5.2 失败的部分 —— 以及为什么

**用户实听评价：效果一般，很多歌词错位。** 根因链：

| 根因 | 证据 |
|---|---|
| **主唱和和声混在一条 stem 里** | LRC 有 9 行括号和声与主唱行时间重叠；Demucs `--two-stems=vocals` 不区分主唱/和声。**这是总闸门，下游所有问题都能追到这条** |
| **可信音高源只覆盖 43%** | 曲线 A（轨1 的 pitchControls，1860 点）有 15 处 >1s 空洞（最大 32s）。A 有数据处偏差 +0.22；A 缺失处退化到曲线 B 则偏差 **+6.90**、粗错 **57.3%** |
| **曲线 B 不可用作主唱音高源** | 3383 点，range 35.1–82.4。对实测 f0 的偏差：中位 +1.75、平均 +6.12、**标准差 7.89**；19% 是八度错；**不是固定音程**（+7±1 只占 2.7%，"平行五度和声"假设不成立）；37% 本来就对 |
| **21% 音符纯属虚构** | 300 个音符里 **63 个（21%）没有任何声学证据**，是从邻居抄的。**用户听到的歌词错位主要就是它们** |

### 5.3 pyin 在这份素材上不可靠（三个实测坑）

1. **气声/强混段误判无声**：49.3–50.5s 处 LRC 确认在唱，`vprob` 中位仅 **0.01**，而 `rms` 中位 −18.8dB（能量充足）、f0 有值 101/103 帧、MIDI 中位 67.90。按 vprob 门限会把真音符当无声丢掉。
2. **锁次谐波**：139.9–140.6s 全帧 f0 中位 **68.90**（正确，对应标注 69），门限筛出的子集中位却是 **50.00** —— 440/146 = 3.0，锁到了三次次谐波。
3. **混叠段跟错声部**：110–113s 稳定 77.9 半音但 LRC 该处无唱词，可能是没记进 LRC 的和声尾音，也可能是器乐渗进 vocals stem。

### 5.4 ⚠️ 评估陷阱（本项目最重要的方法论教训）

**用 f0 定音高，再用同一个 f0 评分，是循环论证。**

上次会话的 v4 报出"整体 73.5% 准确率"，其中 pyin 来源那 79 个音符是"用 pyin 定音高、再用 pyin 评分"，那部分的 90.7% 毫无意义。

**真实数字：**

| 音高来源 | 音符数 | 占比 | 可信度 |
|---|---|---|---|
| 轨5（SynthV DNN 主唱转写） | 152 | 51% | **64.3% 准到 0.5 半音，偏差 −0.42。非循环，可信** |
| 曲线 A | 6 | 2% | 样本太小不评 |
| pyin 实测 | 79 | 26% | **无法独立验证** |
| 邻近推定 | 63 | 21% | **无声学证据，纯占位** |

**当前基线（要超越的对象）：轨 5，174 音符，对实测 f0 在中段 40% 窗口下 72.2% 准到 0.5 半音、偏差 −0.19。** 但它只覆盖部分曲段，歌词是拼音。

### 5.5 线 1 的验收目标（建议，需与用户确认）

《潮声回响》作为**基准曲 / 回归测试用例**。一条"通过"的翻唱管线应同时满足：

| 指标 | 目标 | 当前实测 |
|---|---|---|
| 字级对齐率（直接对齐，不含插值） | **≥ 98%** | 95.2%（混合 stem 上） |
| 音高准确率（**独立估计器**测，非建音符所用的那个） | **≥ 85% 准到 0.5 半音** | 64.3%（仅可信的那一半） |
| 虚构音符 | **0 个。无证据的音符不写入，列进缺口清单** | 63 个（21%） |
| 音符几何 | 0 重叠、时长全部 ≥85ms | 已达成（v4） |
| 人工验收 | 用户完整试听确认无可闻歌词错位 | **未通过** |

**"成功识别《潮声回响》"这个表述不要用** —— 对齐步骤成功 ≠ 管线成功，用户实听是不通过的。写成上面这种可量化形式，既准确又能当回归基准，面试里也站得住。

---

## 6. 已设计但未实现的六阶段管线

每阶段一个硬门槛，**不过不许往下走**。上次失败的元凶就是在没有可信验证的情况下走了太远。

| 阶段 | 做什么 | 门槛 |
|---|---|---|
| **0 素材** | 用 karaoke 模型分离出**独立主唱**（非主唱+和声混合） | 人工试听确认和声基本消失。**不过则这首歌不支持自动转写，回退到人工摆音符** |
| **1 f0** | RMVPE 提 f0（替代 pyin） | **RMVPE 与 torchcrepe 两个独立估计器在有声帧上 ≥85% 一致到 0.5 半音** |
| **2 逐行对齐** | 每行**独立**估 LRC 偏移（上次只做了全局） | 每行残差 <150ms |
| **3 逐字对齐** | CTC（脚本已有，直接复用） | ≥98% 字直接对齐，不靠插值 |
| **4 音符构建** | 起音 + 时长 + 音高，按行摊开保最小间距 | 0 重叠、时长 ≥85ms、**每个音高都有直接声学证据** |
| **5 写入验收** | 通过桥写入 | 用户试听 |

### 6.1 音符构建的已知正确做法

- **时长 = 到下一字起音**（单声部演唱的自然形态），上限 1.70s
- 两字尖峰相距 **<85ms 一定是对齐误差**。处理方式：**按行摊开起音保最小间距 85ms，不要合并** —— 上次用"合并"丢了 43 个字
- 消重叠不要写成 `duration = max(下限, 间隙)`：间隙小于下限时会反而造出重叠（上次 v3 因此产生 64 对重叠、中位 70ms）
- 音高取**音符中段 60%** 的中位（避开边界过渡区），优于全跨度
- **网格量化没有帮助**（16 分音符 snap 实测 64.9% vs 不 snap 66.7%）—— 演唱是表情性的，别硬对网格

### 6.2 组件可得性（已核实）

| 组件 | 状态 | 体积 |
|---|---|---|
| `audio-separator` 0.44.5 | pypi 可装 | 包小，模型按需下 |
| `rmvpe.pt`（歌声专用 f0） | HF `lj1995/VoiceConversionWebUI` | **181.2 MB** |
| `torchcrepe` 0.0.24 | pypi 可装 | 模型 ~90MB |
| `pypinyin` | 未装。**建议装**，用于 OOV 字的同音回退，避免盲插值 | 很小 |

---

## 7. 可复用资产（别重做）

### 7.1 CTC 对齐模型 —— 已下载并校验，别再下一遍

```
路径    E:\SynthV-models\zh-ctc\
模型    jonatasgrosman/wav2vec2-large-xlsr-53-chinese-zh-cn
大小    1,276,296,151 字节
sha256  de031fd4b29e0c0667e5346450fadfe1326c89936b888b59c4ede608db763ee4
架构    Wav2Vec2ForCTC（标准类，保证可加载）
词表    3503，其中 3439 个单汉字 token
覆盖    《潮声回响》用字 170/179 = 95.0%
blank   vocab['<pad>'] = 0
预处理  do_normalize=True, 16kHz
```

**这 1.28GB 下了六次才成功**（静默截断、续传字节错乱、后台进程被回收、并行也只有 164 KB/s）。下载务必：单进程、`-C -` 续传、**下完必须核 sha256**（第一次拿到的文件大了 31,399,936 字节且 sha 不符）。

配套脚本在 `E:\sv-bridge\prototypes\`（含该目录自己的 README 说明边界）：`ctc_align.py`（含自实现 Viterbi）、`respace.py`、`build_final.py`。**这些是上一个会话的一次性原型，能跑但没有工程结构**（无配置、路径硬编码、无测试、无错误处理）。正式实现时按 §1.1 的架构重写，把它们当参考实现和回归对照，不要直接当成模块。

### 7.3 目录布局（已按此整理）

**项目名：`sv-bridge`**（用户在被两次提示与上游 `synthv-agent-bridge` 有混淆风险后仍确认此名。**不要再劝**。缓解措施已落地：README 首屏的归属声明 + 职责分工表 + 仓库 description。）

```
E:\SV_MCP\           上游依赖，git 干净，只 git pull，不在此写代码
E:\sv-bridge\        本项目，将 git init 推 GitHub
  toolkit/ skills/ specs/ specs/adr/ eval/ scripts/ prototypes/
  README.md .gitignore HANDOFF.md
E:\SynthV-models\    1.2GB 模型，不进 git，靠 scripts/ 重建
E:\潮声回响\          基准曲素材，148MB 版权音频 + lyrics.lrc，绝不进 git，按 config 引用
```

`.gitignore` 已建，排除：音视频/stem、`*.lrc`、`*.svp`、模型权重（`*.bin/*.safetensors/*.pt/*.onnx/*.dnni`）、`config.local.*`、`*.npz`。

**`.gitignore` 必须排除：音频/视频/stem（版权）、模型权重（体积 + 第三方）、`lyrics.lrc`（版权）、`.svp` 工程。**

**设计建议**：素材路径写进配置文件而非硬编码，这样基准曲可替换、eval 能跑多首歌 —— 这是"评估框架"和"一次性脚本"的分界。

### 7.2 备选小模型（未验证）

`kehanlu/mandarin-wav2vec2-aishell1`，390.6 MB，词表 4233（4230 单汉字），覆盖率 **99.4%** 更高。但架构是 `Wav2Vec2ForEspnetCTC` **非标准**，CTC 头命名可能对不上导致 `lm_head` 被随机初始化。如果要用，需先确认权重键名能映射。

---

## 8. 线 2（独立创作）

**验收依据用户说后面再给。** 目前只有方向性判断：

- 技术性质与线 1 完全不同 —— 线 1 是信号处理 + 对齐（用户的强项，差异化高）；线 2 更偏 LLM 应用编排（更贴主流岗位 JD，但差异化低）
- 独立创作**不需要转写**：旋律是人写的，歌词与音符的对应关系是已知的。因此上次失败的那整类问题（从混合音频反推音符和音节边界）根本不存在
- 剩下的活恰好全是桥的强项且已验证可用：`add_notes` / `fit_lyrics` / `edit_notes` / `add_pitch_controls`（Smart Pitch）/ `set_automation_points` / `set_group_voice`（Vocal Mode 数值）/ `humanize_notes` / `apply_expression_preset` / 和声用 group reference
- 已装的 `composition-arrangement` 和 `lyric-writing` 技能就是为原创写的，直接可用

**建议先和用户确认线 2 的范围和验收标准，再动手。**

---

## 9. 给下一个会话的工作规则

1. **代码你写，决策他做。** 技术岔路一律"选项 + 取舍 → 他拍板 → 记 ADR"，见 §0.1。不要默默替他选了就往下做。
2. **每阶段设可量化门槛，不过就停下来报告。** 不要在没有可信验证的情况下连做多步 —— 上次就是这么把一个 regression 包装成"93% 提升"的，抽查才发现是错的。
3. **警惕循环验证。** 建音符用的估计器不能同时当裁判。
4. **宁可少交，不可虚构。** 没有证据的音符不写入，列进缺口清单交给用户的耳朵。
5. **别信自己算出来的漂亮数字，先找它的反例。** 上次三个具体错误（丢掉真在唱的字、把正确的 69 改成次谐波 50、把主唱推到可能是器乐渗漏的高八度）全是抽查发现的。
6. **写入前先确认目标工程**。`sv_query get_project_info` 看 `fileName`，别写错工程。
7. **写完提醒用户 Ctrl+S**。桥不能保存。
8. **上游文档会漂移。** 例：桥仓库把《小星星》Demo 移交给 SKILLS 仓库，但那边根本没有 `examples/twinkle-star-demo.json`（该文件在组织下 4 个公开仓库中均不存在，文档指向的 `plugins/synthv-copilot/skills/` 路径布局也不存在）。以实测为准，不要相信文档。
9. **《潮声回响》工程的安全约束**：原工程 `潮声回响-星尘.svp` 只读；不要重跑迁移脚本；每阶段另存版本号不要原地覆盖。

---

## 10. 立即可做的第一步（建议）

和用户确认范围之后，从**阶段 0** 开始 —— 它是整套东西的总闸门，也是唯一能判断"这首歌到底能不能自动翻唱"的门槛。如果分离不出干净主唱，结论就是素材不支持自动转写，应该回到「用户按耳朵摆音符 + agent 做调声（歌词/音素/Pit/参数/和声）」的分工。**那条路本次会话已验证是稳的：桥的写入零失败。**

同时建议早点把项目范围从"完善所有功能"收敛到一条可完成、可讲清的主线 —— 这是简历项目最大的风险点。
