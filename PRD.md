# SV-Agent v2.2 · 产品需求文档

> 给**创作者本人**看。不含代码。**图优先，文字只做图的注解。**
>
> 2026-09-23　状态：**待逐条拍板。一行代码都还没写。**
>
> **这是唯一主文档。** 它合并了两份前稿，两份都作废：
> - `PRD.md` v2（2026-09-22，我起草）—— 保留：全部图、四条构建纪律、双向学习的拆解
> - `SV-Agent-v2.1-PRD.md`（评审稿）—— 保留：R01–R14、T01–T12、三段式性能指标、
>   V0 风险表、发布平台规划，以及十条修正里的九条
>
> **两处我没照收**，改法见 §6.4 和 §16.1，都标了理由。
>
> 两份文档并存迟早漂移 —— 上一个项目为此栽过：同一个数在两个入口各算一遍，
> 一个报 0.340、一个报 0.360，**两边都不报错**。

---

# 0. 铁律

> ## **这个项目只是为了让我能更好、更快地产出虚拟歌手歌曲作品。**
>
> —— 创作者，2026-09-24

**这一条压倒本文档其余全部内容。** 任何条款与它冲突，改的是那个条款，不是它。

## 0.1 怎么用它

每一项功能、每一条判据、每一个里程碑，都必须能回答下面两个问题之一：

```
   ┌──────────────────────────────┬──────────────────────────────┐
   │  它让出歌**更好**吗？          │  它让出歌**更快**吗？          │
   │  作品质量 · 你愿不愿意发出去    │  少等 · 少手动 · 少返工        │
   └──────────────────────────────┴──────────────────────────────┘
                 两个都答不上来 ──► 砍掉，不进任何版本
```

这也是 §17.1 **Q9**（哪些功能现在就砍）的唯一判断标准。

## 0.2 三条直接推论

| # | 推论 |
|---|---|
| **1** | **「agent 内核」是手段，不是目的。** 它存在的理由只有一个：**加新能力时不会把已经跑通的出歌链路弄坏**。它自己不产出任何作品 |
| **2** | **「架构感」不是验收项。** 本文档任何地方都不以「看起来像个 harness」作为做某件事的理由。看到这类论证，按铁律驳回 |
| **3** | **学习也服务于铁律。** 纪律四（慢一点、一步一步）的目的是让你**能掌控和修改这个工具**，不是学习本身 |

## 0.3 和两条判据的关系

```
                        铁律：更好 + 更快
                    ┌─────────┴─────────┐
                    ▼                   ▼
              判据 B「好听」        「更快」这一半
              就是「更好」那一半      ├─ 快速试听（R04）
              §2 · §8.2             ├─ 局部重生成（R07）
                                    ├─ 失败恢复（§9）
                                    └─ 判据 A ← 降为推论 1
                                       内核可扩展不是并列目标，
                                       是为了保住「更快」
                                       不被后续开发拖垮
```

---

## 目录

| 节 | 内容 | 来源 |
|---|---|---|
| **0** | **铁律**（压倒其余全部） | 创作者 2026-09-24 |
| **1** | 产品定位 · 要解决的问题 · 两类验收 | 合并 |
| **2** | 判据 B 的残酷事实（项目的起点） | v2 |
| **3** | 功能全景（56 项，一张图看全） | v2 |
| **4** | 用户旅程 | v2 + v2.1 修正 |
| **5** | 版本范围 V0 → V3 | v2.1 |
| **6** | 反馈与记忆规则 | v2.1 + 一处改法 |
| **7** | V1 需求清单 R01–R14 | v2.1 |
| **8** | 体验与质量验收（三段式指标） | v2.1 |
| **9** | 版本、冲突与失败行为 | v2.1 |
| **10** | 数据与架构边界 | 合并 |
| **11** | 构建思路（六张图） | v2 + v2.1 修正 |
| **12** | 构建纪律（四条，不可协商） | v2 + v2.1 加强 |
| **13** | 发布平台 | v2.1 |
| **14** | V0 必须验证的风险 | v2.1 |
| **15** | 里程碑 M0–M6 | v2.1 + v2 依赖图 |
| **16** | V1 验收场景 T01–T12 | v2.1 |
| **17** | 待拍板 · 未回答的 | 合并 |
| **附录** | 相对 v2 的调整（11 条） | —— |

---

# 1. 产品定位与验收

**SV-Agent 是帮创作者完成「生成 → 试听 → 修改 → 定稿」的虚拟歌手创作助手。**
创作者用自然语言和工程内手改控制作品，系统负责生成候选、执行工具、管理版本，
并保留每次修改的依据。

首期服务创作者本人，单用户、本地工程。长期覆盖原创、翻唱分析、作品管理、
发布准备与 PV。

## 1.1 要解决的五个问题

| 问题 | 产品应提供的结果 |
|---|---|
| 生成结果通过检查，听起来仍不满意 | 尽快听到候选，并方便比较、修改 |
| 改副歌时误伤主歌，或覆盖手工调教 | 明确修改范围，保护未授权内容，冲突交给创作者决定 |
| 评价留在聊天里，下一次无法利用 | 保存评价及其适用范围，确认后形成可撤销的偏好 |
| 一次长流程失败，要从头再来 | 从已完成的检查点恢复，显示失败位置和已产生的费用 |
| 生成了很多文件，分不清对应版本 | 歌曲、候选、试听、工程、执行记录之间可互相追溯 |

## 1.2 两类验收：一个是目的，一个是手段

**这是相对 v2 最重要的一处修正。** v2 把「三个领域跑完」当成成品的前提，
意味着**第一首歌之前拿不到任何成品** —— 那是错的。

```
   产品验收（目的）                     工程验收（手段）
   ┌────────────────────────┐        ┌────────────────────────┐
   │ 目标：降低「拿到满意片段 │ ◄───── │ 目标：加新能力时         │
   │ 和可继续编辑工程」的成本 │  服务于 │ 不把已跑通的出歌链路弄坏 │
   ├────────────────────────┤        ├────────────────────────┤
   │ 证据：实际试听 · 修改记录│        │ 证据：插件接口测试       │
   │ 候选保留率 · 完成作品    │        │ 故障恢复 · 权限 · 运行记录│
   ├────────────────────────┤        ├────────────────────────┤
   │ 何时验收：V1.1 音乐闭环  │        │ 何时验收：M1 起持续验     │
   └────────────────────────┘        └────────────────────────┘
      两条**互不阻塞**（工程验收不许挡住出歌），但**方向是单向的**：
      右边从来不是目的（铁律推论 1）。
      PV 和第三个领域是后续扩展，不是首期完成的前提。
```

### 内核解耦怎么验（v2 的 grep 判据被替换）

v2 写的是「内核 grep 不到『歌/旋律/歌词』= 解耦」。**这是弱判据** ——
把变量名改成 `domainObject` 就能绕过。替换成三条强判据。

**注意它们的理由**：不是「证明这是个 harness」（铁律推论 2 驳回这种论证），
而是「**以后加翻唱、加 PV、换模型的时候，不会把已经能出歌的那条链路弄坏**」。

```
   ✗ 弱判据（保留为辅助检查，不作为证明）
     grep 不到音乐词汇

   ✓ 强判据
     ① 音乐插件卸载后，基础循环仍能跑
     ② 新增一个测试插件，不用改内核任何业务分支
     ③ 假工具能注入：超时 · 取消 · 重复调用 · 权限拒绝 · 中途退出
```

**MCP、多模型、上下文压缩、子 Agent 按实际集成需要安排，都不作为「是不是 Agent」的单独判据。**

## 1.3 首期不承诺

- 自动保证作品好听、达到商业发行水平，或彻底消除重复抱怨
- 自动完成专业混音、完整调教、整首复杂配器和 PV
- 多用户协作、多租户、团队审批、桌面安装器、多宿主兼容
- 自动发布到外部平台
- 外部软件能力未经验证前，承诺全链路无人值守

> 有日志、权限和重试机制，**不等同于**已经具备企业多用户服务能力。

---

# 2. 判据 B 的残酷事实

**这是整个项目的起点，不能在细化过程中被稀释。**

上一个项目：八项检查、七个指标，阈值全部锚在 **POP909（909 首真歌）** 的分布上，
新歌《末班》句极差中位落在真歌第 37 百分位 —— **全绿**。

你听完说：**「节奏型过于简单」**。

> **指标能挡住「明显不行」，挡不住「平庸」。**
> 这不是指标没做好，是**指标这个手段的天花板**。
> 再加十项检查也是同一个结局 —— 它们只能表达「不要犯什么错」，
> 表达不了「要好到什么程度」。

```
            ✗ 错的方向                        ✓ 对的方向
   ┌──────────────────────┐        ┌──────────────────────────┐
   │  造更多指标           │        │  把你耳朵进回路的成本      │
   │  想让机器代替耳朵      │        │  压到一次点击              │
   │  结果：全绿，依然平庸   │        │  再让你的评价能被记住      │
   └──────────────────────┘        └──────────────────────────┘
```

**一处 v2 的错误在这里纠正**：v2 写「节奏型种类 ≥ N」作为改善判据。
**音符更多不等于更好**，那样会长出一堆碎音。
改善只能由「**可比较的候选 + 你的选择**」验证（见 §6.3、§8.2）。

---

# 3. 功能全景（56 项）

> v2 写的「48 项」是**算错的**，实际 11+8+5+4+6+5+5+12 = **56**。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              你（创作者）                                    │
│   一句话 · 一段音频 · 一句评价「节奏太方」· 三处手改（SV / FL / 歌词 txt）   │
└──────────────────────────────────┬──────────────────────────────────────────┘
                                   │
┌──────────────────────────────────┼──────────────────────────────────────────┐
│ 交互面    单一操作界面（聊天 · 试听 · 版本对比）+ Claude Code 对话入口       │
│           两个入口共用同一个会话对象（§16.1）                                │
└──────────────────────────────────┼──────────────────────────────────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         ▼                         ▼                         ▼
 ┌───────────────┐         ┌───────────────┐         ┌───────────────┐
 │ A 创作（正向）│         │ B 翻唱（逆向）│         │ G  PV（V3）   │
 │     11 项     │         │     8 项      │         │     5 项      │
 ├───────────────┤         ├───────────────┤         ├───────────────┤
 │ 主题 → 规格   │         │ 特征指纹      │         │ 分镜          │
 │ 歌词          │         │ 源分离        │         │ 素材          │
 │ 旋律          │         │ 音高转写      │         │ 时间轴对齐    │
 │ 和声          │         │ 逐字对齐      │         │ 歌词条/字幕   │
 │ 节奏          │         │ 调教手法提取  │         │ 渲染导出      │
 │ 编曲配器      │         │ 还原度评测    │         │               │
 │ 调教 · 混音   │         │               │         │               │
 └───────┬───────┘         └───────┬───────┘         └───────┬───────┘
         └─────────────────────────┼─────────────────────────┘
                                   ▼
                    ┌──────────────────────────────┐
                    │  C  共享曲目表示 + 双向互学    │
                    │           5 项                │
                    └──────────────┬───────────────┘
                                   │
         ┌─────────────────────────┼─────────────────────────┐
         ▼                         ▼                         ▼
 ┌───────────────┐         ┌───────────────┐         ┌───────────────┐
 │ D 试听与判断  │         │ E 作品管理    │         │ F 发布准备    │
 │     4 项      │         │     6 项      │         │     5 项      │
 └───────────────┘         └───────────────┘         └───────────────┘
                                   │
┌──────────────────────────────────┼──────────────────────────────────────────┐
│  H  内核（agent 基础设施）12 项 —— 上面每一个方块都跑在它上面                │
│  能力注册 · 权限两道闸 · 执行语义 · 检查点/版本 · 长期记忆                   │
│  可观测性 · 评测门禁 · 多模型 · 人机交接面 · MCP · 子 Agent · 上下文压缩     │
└─────────────────────────────────────────────────────────────────────────────┘
```

**56 项是能力地图，不是 V1 范围。** V1 实际要做的是 §7 的 **R01–R14**。
两者的关系：R01–R14 覆盖 A/C/D/E/H 的一个可用子集，B/F/G 整组不在 V1。

**不把旧功能标成已实现。** v1 有过的代码全部作废，此处一律按「要做」对待。

> **每一项都要过铁律那两问**（§0.1）：它让出歌更好，还是更快？
> 两个都答不上来的直接从地图上划掉。**H 组里最该被这样审一遍的是
> H10 MCP、H11 子 Agent、H12 上下文压缩** —— 它们能过，但理由必须是具体的：
> MCP 是为了挂现成的音频/封面工具不用自己写；子 Agent 是为了 R14 并行试三版；
> 上下文压缩是为了长会话不断片。**不是因为「一个 harness 应该有这些」。**

---

# 4. 用户旅程

## 4.1 主线：从想法到可保留的片段（V1 的全部）

```
你 ──「写一段末班地铁上的独白，副歌克制一点」
     │
     ▼
 ①  编辑规格          主题 · BPM · 调性 · 拍号 · 目标段落        R01
     │                每一项你都能改；变更形成版本
     ▼
 ②  歌词候选 ≥2 版 ───────────────────────────────►  你选/组合  R02
     │                候选有稳定 ID，重启后仍能引用（T01）
     ▼
 ③  旋律 · 和声 · 节奏候选                                       R03
     │                记录输入 · 模型 · 参数
     ▼
 ④  ♪ 快速试听 ───────────────────────────────────►  你听      R04
     │                                                │
     ▼                                                │
 ⑤  A/B 比较（同一乐段 · 同音色 · 同增益）───────────►  你比     R05
     │      │                                         │
     │      └── 要改 ─► ⑥ 自然语言反馈 ─► 回到 ③ ─────┘
     │                    R06 + R07（定范围 · 锁定）
     ▼
 ⑦  保留 → 提交版本 → 导出 MIDI / 试听文件（标注草稿）    R08 · R12
     │
     ▼
 ⑧  进入定稿链路（V1.1）：整曲 · 配器 · 工程 → 你在 SynthV/FL 里做完
```

## 4.2 人工交接：**不承诺「只有固定四个地方需要人」**

v2 画了「★ 四处」的图，**那是理想状态，不准确**。真实规则是按授权范围划分：

```
   ┌─────────────────────────────────────────────────────────┐
   │  已授权范围内 ── 直接执行                                │
   │  生成候选 · 试听 · 在锁定范围外修改 · 读取分析            │
   └─────────────────────────────────────────────────────────┘
                              │
   ┌─────────────────────────────────────────────────────────┐
   │  必须明确确认 ── 四类                                    │
   │  ① 覆盖冲突（外部改过的文件）                            │
   │  ② 扩大修改范围（碰到锁定内容）                          │
   │  ③ 突破预算                                              │
   │  ④ 触发新的外部副作用（上传 · 发布 · 不可撤销的写入）     │
   └─────────────────────────────────────────────────────────┘
```

你可以在任意阶段暂停、试听或编辑。规格与歌词选择、候选保留、冲突处理、
偏好升级、定稿确认**都允许人工参与**。

## 4.3 示例：改副歌节奏（R06 + R07 的实际样子）

1. 你选中副歌后半句：**「节奏太方，前半句和歌词不要动」**
2. 系统显示修改范围，**锁定前半句与歌词**，把反馈解释成可试听的修改方向
3. 给出两个候选 —— 例如改起音位置、改长短音组织。**不以「音符更多」为成功标准**
4. 你在同一乐段比较旧版与候选，选择 / 继续改 / 全部放弃
5. 系统保存选择和原因。**默认只作用于本段**；你确认后才提升为歌曲偏好或长期偏好

---

# 5. 版本范围

```
  V0            V1              V1.1              V2              V3
  可行性验证     创作闭环(MVP)    成品+首批发布      翻唱与学习       管理与扩展
  ──────────►   ──────────────► ────────────────► ──────────────► ─────────────►
  哪些链路能跑   8–16 小节        整曲 · 配器        源分离 · 转写    检索 · 元数据
  哪些要人工     试听改闭环       工程适配           对齐 · 置信度    其余四平台
  环境与兼容     版本 · 偏好      B站 + 网易云       校准库           分镜 · 字幕 · 渲染
                                 两平台实操验证      受控先验
```

| 阶段 | 可交付结果 | 暂不包含 |
|---|---|---|
| **V0** | 明确哪些关键链路能跑、哪些需人工 | 完整产品界面 |
| **V1** | 可反复试听、修改、保存的歌曲片段 | 完整翻唱、PV、专业成品承诺 |
| **V1.1** | 至少一首完整作品 + B站/网易云发布包 | 自动发行、完整 PV |
| **V2** | 原创和转写共用数据结构，参考分析可回溯 | 默认自动纠正所有疑似错音 |
| **V3** | 作品库、其余四平台发布包、PV | 为验证架构强行增加无用途的领域 |

**三件事不后置**：版本保存、手改保护、失败恢复 —— 全在 V1。
V1 就提供项目列表和必要素材索引；V3 才扩展为完整作品管理。

> **待你确认（Q1）**：V1 只做 **8–16 小节片段**，整曲推到 V1.1。
> 这是合理的 MVP 收敛，但你的原始需求是「**一句话出歌**」。
> 我的看法：**接受**。片段闭环能在最短时间内验证「生成→试听→修改」这条回路
> 到底有没有用；整曲只是把同一条回路跑更多遍。

---

# 6. 反馈与记忆规则

**这是 v2.1 相对 v2 最大的一处提前**：v2 把反馈记忆排到 M7，
但它是 V1 的核心价值，**不该后置**。

## 6.1 一条反馈的生命周期

```
你说「副歌后半句节奏太方，前半句别动」
      │
      ▼
 ① 原话存档 ──── 连同：项目 · 版本 · 片段位置
      │
      ▼
 ② 系统解释 ──「起音过于规整，长短音组织单一」──►  你可以改这句解释 ★
      │
      ▼
 ③ 定范围 ──── 本次尝试 / 本片段 / 本歌曲 / 指定风格 / 长期偏好
      │         **默认只作用于本段**
      ▼
 ④ 定强度 ──── 必须满足的约束  ·  尽量遵守的偏好
      │
      ▼
 ⑤ 生成候选 ─── 改起音位置 · 改长短音组织
      │          ✗ 不以「音符更多」为成功标准
      ▼
 ⑥ 你 A/B 比较 ──────────────────────────────►  选 / 继续改 / 全放弃
      │
      ▼
 ⑦ 记结果 ──── 哪次生成采用了它 · 你是否接受 · 是否再次反馈
      │
      ▼
 ⑧ 升级？ ──── **只有你确认**，才从「本段例外」提升为长期偏好 ★
```

一条反馈要存的字段：**原话与来源 · 解释 · 适用范围 · 强度 · 处理方式 · 状态 · 使用结果**。
状态有五种：待确认 / 生效 / 被覆盖 / 已禁用 / 已删除。

## 6.2 冲突处理

**当前明确指令优先于历史偏好。** 用户锁定内容不自动解除；
新指令需要改动锁定内容时，先确认。历史偏好互相冲突时，显示冲突让你选，
并记录本次例外。

> 「再简单一点」**不会**自动删除此前「节奏丰富」的长期偏好 ——
> 它可以只是当前片段的例外。

## 6.3 验收

- 确认一条**仅适用于副歌**的偏好，下次副歌生成记录能看到采用情况，**主歌不自动受影响**（T07）
- 禁用后新任务不再使用；历史记录仍可解释旧结果（T08）
- 要求删除时，从活跃记忆和后续检索中移除；历史产物是否保留原始评价**需明确告知并提供处理入口**

## 6.4 「同一句抱怨再次出现」怎么算 ← **我没照收 v2.1 的改法**

v2 写的是「第二次出现 = 系统失败」。**这条无法归因**，v2.1 的批评是对的：
可能是没召回、解释错了、执行不到位、或范围变了，四种原因要区分。

但 v2.1 改成「记录为改进未达预期，不直接认定整个系统失败」之后，
**这条判据不再有任何失败条件** —— 没有失败条件的判据不是判据。

**v2.2 的写法（两条都要）**：

```
  第二次出现 ──► 必须触发归因，结果必须落到四类之一，并留档
                 ┌──────────────┬──────────────┬──────────────┬──────────────┐
                 │ ① 未召回     │ ② 解释错误   │ ③ 执行不足   │ ④ 范围变化   │
                 │ 记忆里没找到 │ 理解偏了     │ 找到了没做到 │ 你的要求变了 │
                 └──────────────┴──────────────┴──────────────┴──────────────┘
                                        │
  连续三次同一抱怨，且三次归因都是 ① ──► **记忆层不合格**（这是失败条件）
```

**理由**：①是唯一一种「系统本可以做到却没做」的原因。
②③是能力问题，④根本不是问题。只对①设失败线，既能归因，又留了一条能让它不及格的线。

---

# 7. V1 需求清单 R01–R14

**P0 = 缺失则创作闭环无法验收。P1 = P0 稳定后增加，不阻塞首期。**

```
        P0（12 条）────────────────────────────────────────┐
  R01 规格   R02 歌词候选   R03 旋律和声节奏   R04 快速试听  │
  R05 A/B    R06 反馈       R07 局部重生成+锁定 R08 版本恢复 │
  R09 手改保护 R10 偏好记忆  R11 状态与执行记录  R12 导出    │
        ──────────────────────────────────────────────────┘
        P1（2 条）── R13 从旧版本取片段 · R14 多方案试作
```

| 编号 | 优先级 | 需求 | 验收要点 |
|---|---|---|---|
| **R01** | P0 | 新建项目与编辑规格 | 主题、BPM、调性、拍号、目标段落可改；变更形成版本 |
| **R02** | P0 | 歌词候选 | 至少两版有**稳定 ID**；可跨轮引用和组合；来源可追溯 |
| **R03** | P0 | 旋律、和声与节奏候选 | 依据已确认规格生成；记录输入/模型/参数；**允许保留未通过建议性检查的候选** |
| **R04** | P0 | 快速试听 | 候选产生后可播放指定片段；失败有原因与重试入口 |
| **R05** | P0 | A/B 比较 | 默认对齐相同乐段，**相同预览音色和增益**，可切换版本 |
| **R06** | P0 | 自然语言反馈 | 原话、对象、版本、位置与解释一起保存；**模糊反馈不直接变成全局硬约束** |
| **R07** | P0 | 局部重生成与锁定 | 按段落或句修改；保护范围的音乐字段保持不变，**冲突不静默提交** |
| **R08** | P0 | 版本与恢复 | 可回到指定已提交版本；新修改建立分支，历史版本仍可找回 |
| **R09** | P0 | 手改保护 | 发现外部变化则**暂停该写入**；可导入为新版本、另存副本或取消 |
| **R10** | P0 | 基本偏好记忆 | 用户确认后生效；可查看来源/范围、禁用或删除；生成记录列出**实际采用**的偏好 |
| **R11** | P0 | 状态与执行记录 | 显示运行/等待人工/失败/取消/完成；能追溯产物、耗时和已知费用 |
| **R12** | P0 | MIDI 与试听文件导出 | 文件可打开、与选定版本对应；**标注为草稿导出** |
| **R13** | P1 | 从旧版本取片段 | 先显示替换范围和调教冲突，确认后生成新版本 |
| **R14** | P1 | 多方案试作 | 各候选隔离保存；选定后才进入当前版本；**不要求必须用子 Agent 实现** |

---

# 8. 体验与质量验收

> 以下数值是**拟议验收目标，不是实测成绩**。
> V0 需记录机器、模型、音源和网络条件；调整目标要留下原因。

## 8.1 试听性能：三个指标分开算 ← v2 的「30 秒」在这里被修正

v2 只写了「30 秒试听」，**没定义起点** —— 点击试听到出声，
和提交修改到出声（含模型生成），是完全不同的两件事，后者 30 秒根本做不到。

```
  ┌────────────────────────────────────────────────────────────────┐
  │ 标准样本：8–16 小节 · 主旋律与和声 · 单一固定钢琴音源           │
  │           实际秒数随 BPM 变化                                   │
  └────────────────────────────────────────────────────────────────┘

  ① 快速试听耗时   已有候选 ──点击「试听」──► 第一段声音就绪
                   目标：热启动连续 20 次，**至少 19 次 ≤ 30 秒**

  ② 完整生成耗时   提交修改 ──含模型生成──► 新候选可试听
                   **单独统计，不冒充 ① 的 30 秒指标**

  ③ 冷启动         模型和音源加载时间，显示准备状态
                   **V0 后才确定目标**

  ④ 故障统计       超时和失败**必须计入报告**，不能只统计成功请求
```

**钢琴预览只用于判断旋律、和声和节奏。**
发音、咬字、音色、完整配器与混音**必须在定稿试听中判断**。

## 8.2 创作有效性（判据 B 的可操作版本）

用**至少 5 个不同创作意图**，每个完成不少于两轮试听与修改，记录：

- 从开始到得到一个你愿意保留的片段的**总时间**
- 候选数、修改轮数、人工操作时间、模型费用
- 你选择「保留 / 继续改 / 放弃」的结果与**原因**

> **V1 验收线：至少 3 个意图获得你愿意继续制作的片段，其余给出明确失败原因。**

这是单用户可用性验证，**不据此宣称普遍优于其他音乐产品**。V1.1 另以完整作品验收。

## 8.3 检查器纪律

```
   绿 = 本项检查通过
   红 = 发现本项缺陷
   灰 = 缺乏证据 · 未执行 · 无法判断     ← 灰不是绿，不许拿绿灯冒充「我不知道」

   每项都要展示：检查器版本 · 输入版本 · 判断依据
```

**硬性检查**（数据损坏、越界写入）**阻止提交**；
**建议性检查**（风格、旋律复杂度）**允许你明确保留**（R03）。

每个检查器需要两种样本：**注入已知缺陷能检出** + **正常样本不误报**。

真实参考曲与生成作品使用**相同的指标定义及版本**。校准样本按适用风格和数据质量筛选；
**保留独立评估样本，不用新加入的数据同时证明规则更好**。

---

# 9. 版本、冲突与失败行为

```
                              生成 / 写入 中
                                    │
        ┌───────────┬───────────────┼───────────────┬───────────┐
        ▼           ▼               ▼               ▼           ▼
   失败/非法      你取消       外部改了文件      预算耗尽    worker 崩溃
        │           │               │               │           │
        ▼           ▼               ▼               ▼           ▼
   不替换当前   停止后续步骤     另存为候选     不启动新动作  标记在途状态
   保留错误+   在途尽力取消     暂停覆盖       保留已完成    重启后核对产物
   已完成步骤  取消完成前显示                  已花费用不     只重试**可安全
   + 重试入口  「处理中」                      会因回滚消失   重试**的步骤
        │           │               │               │           │
        └───────────┴───────────────┴───────────────┴───────────┘
                                    ▼
              当前版本永远是「完整的旧版」或「完整的新版」
              ✗ 没有半提交状态（T04）
              ✗ 迟到的结果不许覆盖当前版本（T06）
```

| 情况 | 必须如何处理 |
|---|---|
| 修改范围扩散 | 提交前比较差异；**保护字段变化则拒绝提交并说明**（T02） |
| API 限流或暂时故障 | 有上限地重试并显示状态；**不无限重试**；不重复提交不可安全重试的动作 |
| 回到旧版本 | 所选快照作为新的工作基线，保留历史分支；**不承诺撤销外部软件或平台中的既成动作** |
| 旧旋律与新调教冲突 | 显示受影响音符与参数；可整体恢复、只恢复兼容部分或另存分支（T09） |

**三条补充规则：**

- **跨文件产物先写入隔离版本目录**，校验完成后统一切换当前版本引用。
  仅对单个文件改名**不足以**证明整个任务原子完成。
- **幂等**指同一操作标识重试不产生重复提交；
  你主动「再生成一次」使用新的操作标识，**可以**得到不同候选。
- 外部软件不参与锁定时，**哈希检查不能消除所有竞争窗口** ——
  检查到写入之间文件仍可能变。首期**优先导出新文件**，避免后台改写你正在编辑的工程。
  （这一条比 v2 的「写前验哈希」更准，直接绕开了整个竞争问题。）

---

# 10. 数据与架构边界

> 本节只约束职责和接口。具体库、数据库、模型与进程管理方案另写技术设计，引用 R01–R14。

## 10.1 核心数据对象

| 对象 | 必要信息 |
|---|---|
| **Project / Song** | 项目标识、规格、当前版本、素材索引 |
| **SongVersion** | 稳定版本 ID、父版本、**乐段与音符稳定 ID**、歌词、和声、调教、来源 |
| **Artifact** | 文件类型、路径、哈希、所属版本、产生它的任务、校验状态 |
| **Feedback / Preference** | 原话、范围、解释、确认状态与生效记录 |
| **Run / Action** | 输入版本、操作标识、工具版本、状态、耗时、费用、输出和错误 |
| **Capability** | 可用状态、适用版本、限制、**最近验证证据**与人工替代路径 |

共享曲目表示要**预留**多轨、变速、拍号变化、一字多音、一音多字；
V1 可以只实现固定速度的基本子集。**拍位置与实际秒数分开表示**，并有明确转换规则。

## 10.2 三种「置信」分开记 ← v2 的 `confidence` 恒为 1.0 在这里被修正

v2 把三件事塞进一个字段。**生成出来的音符没有「识别置信度」** ——
它没被识别过，那个字段应该是**空**，不是 1。

```
   ┌──────────────────┬──────────────────┬──────────────────┐
   │ 结构合法          │ 音高识别可靠      │ 用户喜欢          │
   ├──────────────────┼──────────────────┼──────────────────┤
   │ 检查器说的        │ 转写器说的        │ 你说的            │
   │ 生成：有值        │ 生成：**空**      │ 两者都有          │
   │ 转写：有值        │ 转写：0.0–1.0     │                  │
   └──────────────────┴──────────────────┴──────────────────┘
        生成与转写**共用音乐结构**，但**分别记录来源与不确定性**
```

## 10.3 分层职责

| 层 | 负责 | 不负责 |
|---|---|---|
| **交互层** | 聊天、范围选择、试听、版本对比、确认 | 自行改写业务状态 |
| **Agent 内核** | 模型与工具循环、参数校验、权限、预算、任务状态、检查点、日志 | 音乐指标与和弦规则 |
| **音乐领域层** | 曲目结构、生成策略、音乐检查、反馈解释 | 绕过内核直接提交未授权变更 |
| **工具适配层** | SynthV、FL、音频处理、模型服务的具体调用 | **隐瞒失败或宣称未经验证的能力** |

## 10.4 内核验收

- 未注册或未授权的工具不能执行；**工具列表过滤之外，执行端再次鉴权**（两道闸）
- 参数不合法时不产生副作用；工具输出需校验，**不能仅凭自报成功提交**
- 新增测试插件无需修改内核业务分支；**音乐插件卸载后基础循环仍可运行**
- 假工具与故障注入覆盖：超时、取消、重复调用、权限拒绝、中途退出
- 工具返回**结构化状态、产物、差异、检查结果和必要说明；有数字时提供数字**

---

# 11. 构建思路

## 11.1 一个动作的生命周期

**八步里没有一步认识「歌」。**

```
  模型说「我要调 gen_melody，参数是 {scope:["副歌后半句"]}」
          │
          ▼
  ① 查注册表 ── 工具存在吗？当前作用域给不给？
          │        └─► 不给：它根本不出现在工具表里（第一道闸）
          ▼
  ② 校验参数 ── JSON Schema
          │        └─► 不合法：返回错误，**不产生任何副作用**
          ▼
  ③ 权限闸  ── 属于 §4.2 的四类之一吗？
          │        └─► 是：挂起，问你（第二道闸）
          ▼
  ④ 预算闸  ── 还剩多少 token / 多少秒？
          │        └─► 不够启动下一步：暂停并保留结果，提示已花费用（T12）
          ▼
  ⑤ 记基线  ── 存下「开始时的文件状态」，用于 ⑦ 的冲突比对
          ▼
  ⑥ 执行    ── 产物先写入**隔离版本目录**
          ▼
  ⑦ 校验    ── ★ 比对基线：外部改过吗？保护字段动了吗？
          │        └─► 有冲突：另存候选，暂停覆盖（T03）
          ▼
  ⑧ 切换引用 ── 校验通过后，统一切换当前版本引用（原子在这一步）
          │
          ▼
  ⑨ 返回    ── ★ 结构化状态 + 产物 + 差异 + 检查结果
                 有数字时给数字：改了 37 个音符 / finding 4→0 / 偏移 −1.6 ms
```

**两处 ★ 的理由：**
- **⑦ 不信自述**：v1 抓到过「动作报 `ok=True`、写出 105879 字节、钩子一个没跑」——
  没入账、没有回退点。**按文件比对，不信动作自己说改了什么。**
- **⑨ 给数字**：模型不需要判断正确性。**模型负责品味，检查负责正确。**

## 11.2 三个领域的对称结构

```
     内核提供的插槽              创作 A          翻唱 B          PV  G
   ┌──────────────────┐     ┌────────────┐ ┌────────────┐ ┌────────────┐
   │ 工具集           │ ──► │ 动作        │ │ 动作        │ │ 动作        │
   │ 状态机           │ ──► │ 规格→片段   │ │ 音频→曲谱   │ │ 曲谱→视频   │
   │ 检查器           │ ──► │ 音乐检查    │ │ 还原度评测  │ │ 时间轴对齐  │
   │ 素材类型         │ ──► │ svp/flp/mid│ │ wav/stem   │ │ png/mp4    │
   │ 能力缺口声明     │ ──► │ FL 渲染?   │ │ 无歌词对齐  │ │ ?          │
   │ 试听渲染器       │ ──► │ 钢琴预览    │ │ 原音频      │ │ 低清预览    │
   └──────────────────┘     └──────┬─────┘ └─────┬──────┘ └─────┬──────┘
                                   └─── C 共享曲目表示 ──────────┘
```

**PV 不是「为了凑第三个领域」而做的。** 它有实际用途（你真的要），
而且它**复用**已有的歌词时间数据，不新造一套。
但它**不是**判据 A 的前提 —— 解耦用 §1.2 的三条强判据验，那些在 M1 就能验。

## 11.3 共享曲目表示

```
Song
 ├── meta          调 · BPM · 拍号 · 曲式
 │                 ★ 拍位置与实际秒数分开存，转换规则显式
 ├── sections[]    主歌1 / 预副 / 副歌 / 间奏 ...
 │    └── lines[]        一句
 │         ├── text          「末班车关上门」
 │         ├── syllables[]   字 → 音素 → 起止时间
 │         └── notes[]       **稳定 ID** · 起点 · 时值 · 音高 · 力度
 │              ├── source          generated | transcribed | manual
 │              ├── pitch_conf      转写才有，生成为空
 │              └── structure_ok    检查器给的
 ├── chords[]      时间 → 和弦
 └── tuning        音高线 · 颤音 · 张力 · 气声（逐参数曲线）

 V1 只实现固定速度的基本子集，但**字段位置要预留**：
 多轨 · 变速 · 拍号变化 · 一字多音 · 一音多字
```

**音符稳定 ID 是 R07/R08/R13 的前提** —— 没有它，「只改副歌后半句」
和「恢复旧旋律但保留调教」都无法表达。

## 11.4 双向校准回路（V2）

```
  ┌──────────────────── 参考曲分析 ──────────────────────┐
  │  转写结果 ──► **候选**校准库 ──► 质量审核 + 适用范围标注 │
  │              ✗ 未经验证的转写不能直接充当真值            │
  └────────────────────────┬─────────────────────────────┘
                           │ 通过审核后才影响生成
                           ▼
              ┌────────────────────────┐
              │  正式校准库 / 分位带     │  ★ 保留独立评估样本
              └───────────┬────────────┘     不用新数据同时证明规则更好
                          │
      ┌───────────────────┴───────────────────┐
      ▼  逆向 → 正向                正向 → 逆向 ▼
┌──────────────────────┐      ┌──────────────────────────┐
│ 生成器的约束与阈值     │      │ 作曲先验用于**排序/建议** │
│  音区 · 句长 · 末音    │      │  ✗ 不因某音不在和弦里     │
│                      │      │     就直接改音            │
└──────────▲───────────┘      │  先在人工标注样本上量      │
           │                  │  正确率 **和误改率**       │
           │  你的一句话       └──────────────────────────┘
           │  （走 §6.1 的八步）
      ┌────┴────┐
      │   你    │
      └─────────┘

  每次规则更新保留版本和**回退入口**
```

**v2 在这里犯过一个我自己纪律里最忌讳的错**：写「非和弦音直接改成和弦音并记账」。
按先验直接改音会引入**误改，而误改是安静的** —— 改完听不出来，也查不出来。
现在改成：先验只做排序和建议，没有正向证据（正确率升、误改率可接受）不启用自动改写。

## 11.5 跨语言边界：TS 主进程 + Python 音频 worker

```
┌──────────────────── Node 主进程 ───────────────────────────────┐
│  内核 · 编排 · 工具注册 · 版本与提交权 · 权限 · 前端            │
│  .svp / .flp 的读写（文件格式活，不是音频活）                   │
│                                                                │
│                  ┌────────────────────────┐                    │
│                  │  audio-worker 客户端    │                    │
│                  └───────────┬────────────┘                    │
└──────────────────────────────┼─────────────────────────────────┘
                               │
┌──────────────────────────────┼─────────────────────────────────┐
│  Python 常驻 worker           ▼                                 │
│   音高估计 · 逐字对齐 · 源分离 · 特征提取 · 预览渲染            │
│   可以返回：音频 · 曲线 · 识别结果 · 置信度 · **算法候选**      │
└────────────────────────────────────────────────────────────────┘

   ★ 边界按**状态所有权**划分，不是「只能返回数字」：
     是否采纳 · 是否提交工程 · 是否请求用户确认 ── 全部由主流程决定
```

v2 写的「Python 只能返回数字」**是错的** —— 音频、曲线、候选本来就该在 worker 侧产生。
正确的线是**提交权**：worker 可以给出任何结果，但**只有主进程能改变当前版本**。

Python 与音频依赖的具体版本组合**在 V0 实测后锁定**，不根据旧描述直接限定（见 §14）。

## 11.6 两条链路：快速试听 vs 定稿

```
                  ┌─── 快速链路（判断用）──────────────────┐
 候选 ────────────►│  固定钢琴音源 → 8–16 小节预览          │──► 你的耳朵
      │           │  目标：快。不评价音色                  │    （R04 · R05）
      │           └────────────────────────────────────────┘
      │
      │           ┌─── 定稿链路（成品用，V1.1）────────────┐
      └──────────►│  整曲 → 配器 → FL 工程 →(你导出)→ wav  │──► 成品
                  │  → .svp → 对齐 → 调教 → 混音            │
                  │  目标：好。慢没关系                    │
                  └────────────────────────────────────────┘
```

**牺牲音色，换回路速度。** 定稿音色永远由 SynthV + FL 出，
但**决定「这版旋律行不行」不需要定稿音色**。

---

# 12. 构建纪律（不可协商）

v1 的代码全删，**这四条纪律保留**。它们是需求，不是遗产。

### 一、只报 0 的检查不算检查

每个检查器要有两种样本：**注入已知缺陷能检出** + **正常样本不误报**。

v1 靠前半条抓到 20 + 5 处缺陷，**没有一处能被「跑一遍看输出」抓到** —— 输出全是正常的。
后半条（误报检查）是 v2.1 补的，v2 漏了。

### 二、三色纪律

**绿 = 有依据说它好　红 = 有依据说它坏　灰 = 还没有可判断的依据。**
不许拿绿灯冒充「我不知道」。一个会说「我不确定」的系统是可用的，自信瞎猜的不是。
每项检查要展示：检查器版本 · 输入版本 · 判断依据。

### 三、阈值锚真歌，不锚自己的作品

> 检查全过，如果是因为阈值照着自己的水平校的，
> 它**结构上不可能**告诉你「离专业水准还差多少」—— 它没见过专业水准。

旧歌只当验证手段，不当判据来源。**另外保留独立评估样本** ——
不用新加入的数据同时证明规则更好（这条是 v2.1 补的，防过拟合）。

### 四、慢一点，一步一步（你的明确要求）

| # | 规则 |
|---|---|
| 1 | **每个里程碑划成你读得完的小批次**，不一口气写两万行 |
| 2 | 每站结束**停下来**：演示 + 状态快照 + 失败案例 + 实际成本 + 下阶段范围 |
| 3 | 验收是「**你能解释关键流程**」，不是「跑通了」 |

---

# 13. 发布平台

```
     V1.1 首批 —— 你已有创作者账号，能做真实工作流验证
   ┌────────────────────────┬────────────────────────┐
   │      Bilibili          │      网易云音乐         │
   ├────────────────────────┼────────────────────────┤
   │ 封面静帧 + 完整音频     │ 纯音频 + 歌词           │
   │ 字幕可选                │ 封面 + 歌曲信息         │
   │ 标题 · 简介 · 标签建议  │                        │
   │ ✗ 不等待完整分镜和 PV   │                        │
   └────────────┬───────────┴───────────┬────────────┘
                └──── 同一个已确认成品版本 ────┘
                  平台文案各自独立保存
                  改某平台文案 ✗ 不回写音乐内容
                  音频或歌词变了 ──► 相关发布包标记「待重新生成或复核」

     V3 后续 —— 先保留需求，不同步开发全部适配器
   ┌─────────┬─────────┬──────────────┬─────────┐
   │  抖音   │ TikTok  │   YouTube    │ 小红书  │
   │ 选段视频 │ 选段+多语│ 长视频+短片  │ 视频+图文│
   └─────────┴─────────┴──────────────┴─────────┘
```

**以上是产品输出规划，不代表已核实的平台接收格式、限制或授权要求。**
编码、采样率、画幅、文件大小、歌词格式及必填字段，
在对应阶段**基于官方资料和你本人账号实操验证后**记录到平台配置，**不能凭经验硬编码**。

## 13.1 发布流程

```
 ① 你确认 ──── 成品 · 目标平台 · 署名 · 发布信息
       ▼
 ② 系统生成 ── 发布包 + 文件清单 + 版本 + 可检测问题 + 待人工确认项
       ▼
 ③ 你上传 ──── 本人账号；检查试听、画面、歌词、元数据
       │        记录缺项、失败原因和修复结果
       ▼
 ④ 你决定 ──── 是否正式提交
       ▼
 ⑤ 系统记录 ── 草稿 / 已提交 / 审核中 / 已发布 / 被退回
                **人工录入的状态要标注来源**
                平台审核未结束时只能报「已提交/审核中」，✗ 不能计作已发布
```

**账号已有 ≠ 所有权限、接口与内容审核条件都满足** —— V0 核实实际入口。
账号凭证**不写入项目文件或日志**。本次 PRD 编写不执行账号访问、上传或发布。

---

# 14. V0 必须验证的风险

> **原稿关于外部工具和环境的陈述，一律视为待核实假设。**
> 本稿未声称已完成官方资料核查或实机测试。

| 验证项 | 需要的证据 | 不满足时的方案 |
|---|---|---|
| FL 工程读写、**tempo**、渲染入口 | 指定版本实测；**至少两个不同 BPM 样本**；记录公开接口依据 | 输出 MIDI + 操作指引，人工装配或导出 |
| SynthV 工程、脚本或桥接能力 | 指定产品版本验证，记录写入与渲染限制 | 文件交接、人工打开与导出 |
| 文件并发编辑 | 外部保存与系统导出的冲突实验 | 新文件导出与显式合并 |
| 所谓 **512 音符限制** | 验证这个限制属于哪个桥接版本或接口 | 分批调用并检查边界，或换已验证路径 |
| Python 与音频依赖 | 独立环境安装、加载和样本运行结果 | 锁定可用版本组合，**不根据旧描述直接限定** |
| 首批平台发布入口 | 本人 B站/网易云账号的实际入口、可提交素材、必填信息与状态流程 | 先生成素材包与人工操作清单，记录阻塞项 |
| 钢琴预览速度 | 冷热启动数据、失败率、播放结果 | 调整渲染方式或缩小首期片段范围 |

> **FL 的 tempo 是最该先测的一个。** 失败形态完全符合这个项目最怕的模式：
> 写出的工程速度不对，**文件照样能打开**，人声和伴奏就是对不上，
> 排查时会先去怀疑对齐模块。v1 的两首歌恰好同为 66 BPM，把这颗雷盖住了。

---

# 15. 里程碑

```
 M0 可行性与范围确认
  └─► M1 最小内核与版本
        └─► M2 首个可试听候选
              └─► M3 反馈修改闭环  ★V1 验收
                    └─► M4 成品与首批发布  ★V1.1 验收
                          ├─► M5 翻唱与校准  ★V2
                          └─► M6 管理 · 多平台 · PV  ★V3
```

| 里程碑 | 可演示产物 | 退出条件 |
|---|---|---|
| **M0** | 验证记录、能力矩阵、V1 清单 | 预览有可行路径；工程风险有替代方案 |
| **M1** | 假工具执行、日志、取消、保存恢复 | 权限、故障、重复操作和版本提交测试通过 |
| **M2** | 规格 → 歌词 → 旋律和声 → 钢琴试听 | **实际可以试听**，产物与版本对应 |
| **M3** | A/B、局部修改、锁定、偏好 | 完成 §8 的 V1 验收；核心安全性案例全部通过 |
| **M4** | 整曲、工程适配、B站视频包、网易云音频歌词包 | 满足 V1.1 成品验收，记录两平台账号实操结果与审核状态 |
| **M5** | 带置信度的分析、校准审核与回退 | 评估显示收益，且**误改可见、可撤销** |
| **M6** | 作品检索、其余四平台适配、字幕与视频 | 各功能有独立使用价值与验收记录 |

**不在本稿虚构排期。** M0 完成后，根据实际依赖与每周可投入时间估算 M1–M3；
后续按范围变化滚动修订。

**每站结束提供**：演示 · 状态快照 · 失败案例 · 实际成本 · 下阶段范围。
你能解释关键流程后再继续（纪律四）。

---

# 16. V1 验收场景 T01–T12

**这是把纪律落成动作的地方。** T01–T08、T10–T12 是 V1 必须通过的；
T09 随 R13 上线验收。

| 编号 | 操作或注入条件 | 预期结果 |
|---|---|---|
| **T01** | 重启后引用歌词候选 A，并取 B 的第二段 | 引用仍准确，新版本记录组合来源 |
| **T02** | 只改副歌后半句 | 主歌和锁定字段不变，差异可查 |
| **T03** | 生成期间在外部修改工程 | 候选不覆盖外部修改，提示冲突 |
| **T04** | 提交过程中终止进程后重启 | 当前版本为**完整旧版或完整新版**，无半提交 |
| **T05** | 同一操作标识重试 | 不产生重复版本提交；不可判定的外部状态先核对 |
| **T06** | 取消后工具晚返回 | 当前版本**不被迟到结果覆盖** |
| **T07** | 保存仅适用于副歌的偏好，再生成主歌 | 不错误套用；实际采用项可追溯 |
| **T08** | 禁用偏好或给出相反的当前指令 | 按本次指令执行，记录例外或禁用结果 |
| **T09** | 恢复旧旋律，但调教引用已变化音符 | **不静默声称完整保留**；展示冲突与可选处理 |
| **T10** | 工具请求越权写入或参数非法 | 拒绝执行，记录原因，**无文件副作用** |
| **T11** | 输入无法评价的音乐特征 | 显示**灰色与原因**，不显示通过 |
| **T12** | 预算不足以启动下一步骤 | 暂停并保留结果；提示已花费用和后续选择 |

**人工音乐满意度另按 §8.2 记录，不能由这些工程测试代替。**

## 16.1 首期界面 ← **我没照收 v2.1 的改法**

v2.1 写「单一操作界面，不同时建设 CLI、聊天和独立仪表盘」。
**收敛方向对，但砍得过头了** —— 你现在的实际用法就是**在 Claude Code 里说话**。
如果只有一个 Web 界面，你最顺手的那条入口反而没了。

**v2.2 的写法：**

```
   ┌──────────────────────┐        ┌──────────────────────┐
   │  单一 Web 操作界面    │        │  Claude Code 对话入口 │
   │  试听 · A/B · 版本树  │        │  说人话下指令          │
   │  反馈 · 确认          │        │                      │
   └──────────┬───────────┘        └──────────┬───────────┘
              └────────── 同一个会话对象 ──────┘
                   ★ 前端只渲染，不计算
                     需要一个新数字就去库里加
```

**不做的是第三个**：独立 CLI 仪表盘。
**理由**：前端各算各的，迟早算出两个不同的值而且两边都不报错 —— v1 栽过这个。
两个入口共用同一个会话对象，这个风险就不存在。

---

# 17. 待你拍板 · 未回答的

## 17.1 要你拍板的

| # | 问题 | 建议默认值 |
|---|---|---|
| **Q1** | V1 只做 **8–16 小节片段**，整曲推到 V1.1？ | **接受**（§5） |
| **Q2** | 首期界面：Web + Claude Code 两个入口，砍掉独立 CLI 仪表盘？ | **接受**（§16.1） |
| **Q3** | 预览音色：固定钢琴音源 | 接受 |
| **Q4** | 长期偏好：**你确认后**才生效 | 接受 |
| **Q5** | 外部工程写入：**优先另存新文件** | 接受 |
| **Q6** | 发布方式：准备发布包，**由你发布** | 接受 |
| **Q7** | 「连续三次归因都是未召回 = 记忆层不合格」这条失败线 | 接受（§6.4） |
| **Q8** | 你说的 "pi" 指哪个？`pi-agent`（Rust）还是别的 coding agent？ | **仍需你给信息** |
| **Q9** | 56 项里哪些是你**现在就确定不要的**？ | 按 §0.1 那两问筛；砍得越早越省事 |

## 17.2 开工前另需确定（属于实施输入，不是本稿已确认的事实）

验收机器与软件版本 · 可用模型和音源 · 单任务预算上限 · 每周投入时间。

## 17.3 这份文档没有回答的

- **具体技术选型**（哪个源分离模型、数据存哪、前端框架）—— 等 M0/M1
- **UI 设计** —— 交互形态定了才谈得上画面
- **成本估算** —— 要先有 M1 之后的真实用量基线
- **「好听」的正面定义** —— 见 §2。我认为它不该被定义，该被**快速验证**（§8.2）

---

# 附录：相对 v2 的调整（11 条）

| # | v2 原稿 | v2.2 | 谁提的 |
|---|---|---|---|
| 1 | 三个领域跑完才算成品 | 产品闭环与内核扩展**分别验收** | v2.1 |
| 2 | grep 不到「歌」= 解耦 | 降为辅助检查；改用三条强判据 | v2.1 |
| 3 | 同一句抱怨两次即失败 | 四类归因 **+ 保留一条失败线**（连续三次「未召回」） | v2.1 + **我的改法** |
| 4 | 节奏丰富度增加即改善 | 以**可比较候选与你的选择**验证改善 | v2.1 |
| 5 | 非和弦转写音必须纠正 | 先验只做排序/建议；**量误改率**，无证据不自动改写 | v2.1 |
| 6 | 生成置信度恒为 1 | 来源 / 识别置信度 / 质量判断**分三个字段** | v2.1 |
| 7 | 反馈记忆排到 M7 | **V1 就要有**确认、范围、应用与撤销 | v2.1 |
| 8 | 「30 秒」含义不清 | 分开计算**预览 / 生成 / 冷启动** | v2.1 |
| 9 | 所有动作可回滚 | 区分本地版本恢复与**外部已发生副作用** | v2.1 |
| 10 | Python 只能返回数字 | 按**状态所有权**划分；worker 可返回候选，主进程拥有提交权 | v2.1 |
| 11 | 单一操作界面 | Web + Claude Code **两个入口**，共用同一会话对象 | **我的改法** |

**另外两处事实修正**：v2 写的「48 项」实际是 **56 项**；
v2 的「写前验哈希」改为「**优先导出新文件**」—— 哈希检查消不掉竞争窗口。

---

*本文档用于与创作者讨论需求。所有「建议默认值」都是待否决的提案，不是结论。*
