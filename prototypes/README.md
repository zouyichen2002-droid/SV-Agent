# 原型脚本（参考实现，非交付代码）

来自 2026-08-20 的一次性探索会话。**能跑，但没有工程结构**：路径硬编码、无配置、
无测试、无错误处理、命名混乱。

保留原因只有一个：`ctc_align.py` 里的 CTC 强制对齐 + 自实现 Viterbi 是**验证过有效**的
（33/33 行、300/315 字、11 秒），正式实现时可作参考与回归对照。

**不要直接当模块引用。** 按 HANDOFF.md §1.1 的架构重写。

- `ctc_align.py`   逐行 CTC 强制对齐，含不依赖 torchaudio 的 Viterbi 实现
- `respace.py`     按行摊开起音保最小间距 + 音高来源优先级
- `build_final.py` 从对齐尖峰构建音符 + 分组质量统计
